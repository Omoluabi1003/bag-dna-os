import type { KnowledgeGraphOutput } from "@/lib/intelligence";export function KnowledgeGraphView({graph}:{graph:KnowledgeGraphOutput}){return <div className="glass p-5"><p className="text-[10px] uppercase tracking-widest text-gold">Knowledge Graph</p><div className="mt-4 flex flex-wrap gap-2">{graph.nodes.slice(0,12).map(n=><span key={n.id} className="rounded-full border border-white/10 bg-white/[.04] px-3 py-2 text-[11px] text-ivory">{n.label}</span>)}</div><div className="mt-4 grid gap-2 md:grid-cols-3">{graph.relationshipSummary.map(s=><p key={s} className="rounded-xl bg-white/[.03] p-3 text-xs text-mist">{s}</p>)}</div></div>}
