import type { CollectiveIntelligenceOutput } from "@/lib/intelligence";export function CollectiveNetworkView({network}:{network:CollectiveIntelligenceOutput}){return <div className="glass p-5"><p className="text-[10px] uppercase tracking-widest text-gold">Collective Intelligence Network</p><div className="mt-4 flex flex-wrap gap-2">{network.airportsImpacted.map(a=><span key={a} className="rounded-full bg-cyan/10 px-3 py-2 text-xs text-cyan">{a}</span>)}</div><ul className="mt-4 space-y-2 text-xs text-mist">{network.sharedSignals.map(s=><li key={s}>• {s}</li>)}</ul><p className="mt-4 text-xs text-emerald-300">{network.recommendedNetworkAction}</p></div>}
