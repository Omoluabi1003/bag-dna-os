import { AppShell } from "@/components/shell";import { Badge, SectionHeading } from "@/components/ui";import { eventGraphSeed } from "@/lib/data/eventGraphSeed";
export default function EventGraphPage(){return <AppShell title="Temporal Event Graph" eyebrow="Proof story"><section className="glass p-7"><SectionHeading eyebrow="Check-in to custody closure" title="Chronological evidence trail"/><div className="relative mt-8 space-y-4 before:absolute before:left-5 before:top-0 before:h-full before:w-px before:bg-cyan/30">{eventGraphSeed.map((e,i)=><article key={e.title} className="relative ml-12 rounded-3xl border border-white/10 bg-white/[.035] p-5"><span className="absolute -left-[42px] top-6 grid h-10 w-10 place-items-center rounded-full border border-cyan/40 bg-[#082033] text-xs text-cyan">{i+1}</span><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="font-display text-xl text-ivory">{e.title}</h3><Badge tone={e.result.includes("Review")?"amber":"emerald"}>{e.result}</Badge></div><div className="mt-3 grid gap-3 text-[12px] text-mist sm:grid-cols-3"><p>Timestamp <b className="text-ivory">{e.time}</b></p><p>Integrity impact <b className="text-ivory">+{e.impact}</b></p><p>Evidence hash <b className="font-mono text-cyan">{e.hash}</b></p></div></article>)}</div></section></AppShell>}
