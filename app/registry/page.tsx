import { AppShell } from "@/components/shell"; import { BagRegistryTable } from "@/components/registry/BagRegistryTable";
export default function Page(){return <AppShell title="Bag Identity Registry" eyebrow="MIA · FLL · ATL demonstration corridor"><header className="mb-6"><p className="text-[10px] font-bold uppercase tracking-widest text-gold">DEMO DATA · 36 persistent identities</p><h2 className="mt-2 text-3xl font-semibold">Search, inspect and issue bag identities</h2><p className="mt-2 max-w-3xl text-sm text-mist">Masked, synthetic passenger references and deterministic bag records demonstrate an operational registry without representing airline data.</p></header><BagRegistryTable/></AppShell>}
