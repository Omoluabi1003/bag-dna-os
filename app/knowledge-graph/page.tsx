import { AppShell } from "@/components/shell";import { Badge, SectionHeading } from "@/components/ui";import { knowledgeGraphEdges, knowledgeGraphNodes } from "@/lib/data/knowledgeGraphSeed";
const pos=[[50,8],[50,22],[50,36],[20,52],[40,56],[60,56],[80,52],[25,73],[50,73],[75,73],[25,90],[50,90],[75,90],[50,104]];
export default function KnowledgeGraphPage(){return <AppShell title="BAG-DNA Knowledge Graph" eyebrow="Relationship intelligence"><section className="glass p-7"><SectionHeading eyebrow="Linked records create trust" title="Passenger-to-ledger relationship map" action={<Badge tone="cyan">SVG / CSS only</Badge>}/><p className="max-w-3xl text-sm leading-7 text-slate-200">The graph shows how identity trust is built from connected passenger, bag, tag, seal, flight, airport, handler, custody, alert and ledger evidence records.</p><div className="mt-8 overflow-x-auto rounded-3xl border border-white/10 bg-[#061421] p-6"><svg viewBox="0 0 100 112" className="min-h-[620px] w-full min-w-[760px]">{knowledgeGraphEdges.map(([a,b])=>{const ai=knowledgeGraphNodes.indexOf(a),bi=knowledgeGraphNodes.indexOf(b);return <line key={a+b} x1={pos[ai][0]} y1={pos[ai][1]} x2={pos[bi][0]} y2={pos[bi][1]} stroke="rgba(110,216,224,.42)" strokeWidth=".25"/>})}{knowledgeGraphNodes.map((n,i)=><g key={n}><circle cx={pos[i][0]} cy={pos[i][1]} r="4.2" fill="rgba(212,175,55,.18)" stroke="#d4af37" strokeWidth=".35"/><text x={pos[i][0]} y={pos[i][1]+7.5} textAnchor="middle" fill="#f7f2e8" fontSize="2.6">{n}</text></g>)}</svg></div></section></AppShell>}
