import {AppShell} from "@/components/shell";import {RiskQueue} from "@/components/risk/RiskQueue";export default function Page(){return <AppShell title="Risk Operations" eyebrow="Human-governed decision support"><header className="mb-6"><p className="text-[10px] font-bold uppercase tracking-widest text-gold">DEMO DATA · operator review required</p><h2 className="mt-2 text-3xl font-semibold">Anomaly intervention queue</h2></header><RiskQueue/></AppShell>}
