import { BadgeCheck, LockKeyhole } from "lucide-react";
import { AppShell } from "@/components/shell";
import { primaryDemoBag } from "@/lib/data/demoWorkflow";
export default function ClaimVerificationPage(){const event=primaryDemoBag.custodyEvents.at(-1)!;return <AppShell title="Passenger Claim Verification" eyebrow="Final identity proof · Close custody"><div className="mx-auto max-w-5xl"><section className="glass p-6 md:p-8"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-[10px] uppercase tracking-widest text-gold">Protected release</p><h2 className="mt-2 text-3xl font-semibold">Correct passenger. Correct bag.</h2><p className="mt-2 text-sm text-slate-300">Claim authorization reconciles passenger credentials with the persistent BAG-DNA identity before custody is closed.</p></div><LockKeyhole className="text-cyan" size={38}/></div><div className="mt-7 grid gap-3 sm:grid-cols-2">{[["Passenger claim token","CLM-OP-BA074-12A"],["Boarding pass reference",primaryDemoBag.passenger.boardingPassRef],["BAG-DNA ID",primaryDemoBag.bagDnaId],["Identity match result","VERIFIED"],["Bag Integrity Score","98 / 100 · Verified"],["Seal status","Intact · NFC verified"],["Final custody event",`${event.checkpoint} · ${event.eventHash}`],["Custody closed status","CLOSED · passenger accepted"]].map(([a,b])=><div key={a} className="rounded-2xl border border-white/[.08] bg-white/[.03] p-4"><p className="text-[9px] uppercase tracking-wider text-slate-300">{a}</p><b className="mt-2 block text-sm text-white">{b}</b></div>)}</div><div className="mt-6 flex items-center gap-3 rounded-2xl border border-emerald-400/25 bg-emerald-400/[.08] p-5"><BadgeCheck className="text-emerald-300"/><div><b className="text-emerald-200">Custody closed with verified identity</b><p className="mt-1 text-xs text-slate-300">The release event is linked to the evidence ledger and passenger claim token.</p></div></div></section></div></AppShell>}
