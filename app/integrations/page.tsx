import {AppShell} from "@/components/shell";import {IntegrationMatrix} from "@/components/integrations/IntegrationMatrix";export default function Page(){return <AppShell title="Integration Control" eyebrow="Public, demo and authorized data boundaries"><header className="mb-6"><h2 className="text-3xl font-semibold">System connection matrix</h2><p className="mt-2 text-sm text-mist">Public feeds are fetched in the browser. Production airport and airline systems require stakeholder authorization.</p></header><IntegrationMatrix/></AppShell>}
