import { Database, ExternalLink } from "lucide-react";
import { AppShell } from "@/components/shell";
import { Badge, SectionHeading } from "@/components/ui";
import { publicDatasetCatalog } from "@/lib/data/publicDatasetCatalog";
export default function DatasetIntelligencePage(){return <AppShell title="Public Intelligence Sources" eyebrow="Public trust infrastructure"><section className="glass p-7"><SectionHeading eyebrow="Live where possible · fallback always" title="Public sources converted into decisions, scores and operational actions"/><p className="max-w-3xl text-sm leading-7 text-slate-200">These sources are converted into operational context for baggage identity, airport readiness, corridor risk, digital twins and evidence narratives. Discovery-only sources are clearly marked and never treated as live enforcement data.</p><div className="mt-6 grid gap-4 lg:grid-cols-2">{publicDatasetCatalog.map(s=><article key={s.id} className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-cyan/10 text-cyan"><Database size={18}/></div><div><h3 className="font-display text-xl text-ivory">{s.name}</h3><p className="mt-1 text-[11px] uppercase tracking-wider text-gold">{s.category}</p></div></div><Badge tone={s.mode==="live"?"emerald":s.mode==="discovery"?"cyan":"amber"}>{s.mode}</Badge></div><p className="mt-4 text-sm leading-6 text-slate-200">{s.productValue}</p><div className="mt-4 grid gap-3 text-[11px] text-mist sm:grid-cols-2"><p><b className="text-ivory">Decision improved:</b> whether extra verification is needed.</p><p><b className="text-ivory">Score influenced:</b> trust, airport reputation and custody confidence.</p><p><b className="text-ivory">Operational action:</b> {s.nextStep}</p><p><b className="text-ivory">Powers:</b> {s.powers.join(", ")}</p></div><p className="mt-4 rounded-2xl bg-black/20 p-3 text-[12px] leading-5 text-slate-200">{s.explanation}</p><a className="mt-4 inline-flex items-center gap-2 text-[11px] font-bold uppercase tracking-wider text-gold" href={s.url}>Source context <ExternalLink size={12}/></a></article>)}</div></section></AppShell>}
