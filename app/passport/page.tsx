import { BadgeCheck, Plane, ShieldCheck } from "lucide-react";
import { AppShell } from "@/components/shell";
import { primaryDemoBag } from "@/lib/data/demoWorkflow";
export default function PassportPage(){return <AppShell title="BAG-DNA Passport" eyebrow="Passenger-facing verified baggage identity"><div className="mx-auto max-w-5xl"><section className="overflow-hidden rounded-[30px] border border-white/10 bg-gradient-to-br from-[#143650] to-[#071522] shadow-2xl"><div className="border-b border-white/10 p-7 md:p-9"><div className="flex justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[.22em] text-gold">BAG-DNA Passport</p><h2 className="mt-3 text-3xl font-semibold">{primaryDemoBag.passenger.name}</h2><p className="mt-2 font-mono text-cyan">{primaryDemoBag.bagDnaId}</p></div><ShieldCheck className="text-cyan" size={44}/></div></div><div className="grid gap-6 p-7 md:grid-cols-2 md:p-9"><div><p className="text-[10px] uppercase tracking-widest text-slate-300">Route</p><p className="mt-2 flex items-center gap-3 text-xl font-semibold">{primaryDemoBag.flight.origin}<Plane size={17} className="text-cyan"/>{primaryDemoBag.flight.destination}</p></div><div><p className="text-[10px] uppercase tracking-widest text-slate-300">Integrity Score</p><p className="mt-2 text-3xl font-semibold">98 <span className="text-sm text-emerald-300">Verified</span></p></div><div className="md:col-span-2 grid gap-3 sm:grid-cols-2">{[["Seal history","Activated at check-in · intact at every checkpoint"],["Custody history",`${primaryDemoBag.custodyEvents.length} linked verified events`],["Last verified scan",primaryDemoBag.custodyEvents.at(-1)!.checkpoint],["Claim readiness","Passenger token and boarding pass ready"]].map(([a,b])=><div key={a} className="rounded-2xl border border-white/[.08] bg-white/[.04] p-4"><p className="text-[9px] uppercase tracking-wider text-slate-300">{a}</p><b className="mt-2 block text-sm">{b}</b></div>)}</div><div className="md:col-span-2 flex items-center gap-3 rounded-2xl border border-emerald-400/25 bg-emerald-400/[.08] p-5"><BadgeCheck className="text-emerald-300"/><div><b className="text-emerald-200">Identity protected</b><p className="mt-1 text-xs text-slate-300">Digital credentials and physical bag DNA remain consistent from issuance to claim.</p></div></div></div></section></div></AppShell>}
