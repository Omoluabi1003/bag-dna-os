import { ShieldAlert } from "lucide-react";import { AppShell } from "@/components/shell";import { Badge, SectionHeading } from "@/components/ui";import { threatPatternSeed } from "@/lib/data/threatPatternSeed";import { publicDatasetCatalog } from "@/lib/data/publicDatasetCatalog";
export default function ThreatIntelligencePage(){return <AppShell title="Threat Intelligence Exchange Preview" eyebrow="Simulated patterns · public-data context"><section className="glass p-7"><SectionHeading eyebrow="Not law-enforcement data" title="Anonymized baggage integrity patterns"/><p className="max-w-3xl text-sm leading-7 text-slate-200">These examples are simulated intelligence patterns showing how BAG-DNA OS would reason about stolen tags, substitution, custody gaps, staff anomalies and high-risk corridor movement without exposing sensitive technical details.</p><div className="mt-6 grid gap-4 lg:grid-cols-2">{threatPatternSeed.map(p=><article key={p.title} className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><div className="flex items-start justify-between"><div className="flex gap-3"><div className="grid h-10 w-10 place-items-center rounded-2xl bg-red-400/10 text-red-300"><ShieldAlert size={17}/></div><div><h3 className="font-display text-xl text-ivory">{p.title}</h3><p className="text-[11px] text-gold">{p.type}</p></div></div><Badge tone={p.severity==="Critical"?"red":p.severity==="High"?"amber":"cyan"}>{p.severity}</Badge></div><ul className="mt-4 space-y-2 text-sm text-slate-200">{p.signals.map(s=><li key={s}>• {s}</li>)}</ul><p className="mt-4 rounded-2xl bg-black/20 p-3 text-[12px] text-mist">Operator response: {p.action}</p></article>)}</div></section><section className="glass mt-6 p-7"><SectionHeading eyebrow="Open data mapped to threat context" title="Public sources that support risk explanations"/><div className="grid gap-3 md:grid-cols-3">{publicDatasetCatalog.filter(s=>s.powers.includes("/threat-intelligence")||s.powers.includes("/risk")).map(s=><div key={s.id} className="rounded-2xl border border-white/10 p-4"><Badge tone={s.mode==="live"?"emerald":"cyan"}>{s.mode}</Badge><h3 className="mt-3 text-sm font-semibold text-ivory">{s.name}</h3><p className="mt-2 text-[11px] leading-5 text-mist">{s.productValue}</p></div>)}</div></section></AppShell>}
