import { intelligenceCase } from "@/lib/data/intelligenceSeed";import { calculateIntegrityScore } from "./integrityScoreEngine";
export type GraphNode={id:string;label:string;type:string;score?:number};export type GraphEdge={from:string;to:string;label:string;impact:number};export type TrustGraphOutput={trustNodes:GraphNode[];trustEdges:GraphEdge[];trustScore:number;trustBreaks:string[];positiveTrustSignals:string[];negativeTrustSignals:string[];explanation:string;recommendedAction:string};
export function buildTrustGraph(input= intelligenceCase):TrustGraphOutput{const i=calculateIntegrityScore(input);const nodes=["Passenger","BAG-DNA ID","Bag physical profile","RFID","NFC","QR token","Tamper seal","Flight","Airport","Checkpoint","Staff handler","Custody event","Weather context","Aircraft movement","Evidence ledger"].map((label,idx)=>({id:`n${idx}`,label,type:label,score:idx===1?i.score:undefined}));const edges:GraphEdge[]=[['n0','n1','owns identity',6],['n1','n3','binds RFID',7],['n1','n4','binds NFC',7],['n1','n5','validates QR',5],['n2','n11','weight + visual proof',8],['n6','n11','seal intact',8],['n10','n9','assigned to correct zone',4],['n11','n14','hashes evidence',6],['n12','n11','weather pressure',-input.weatherPressure],['n13','n7','movement agrees',3]].map(([from,to,label,impact])=>({from:String(from),to:String(to),label:String(label),impact:Number(impact)}));return{trustNodes:nodes,trustEdges:edges,trustScore:i.score,trustBreaks:i.negativeFactors,positiveTrustSignals:i.positiveFactors,negativeTrustSignals:i.negativeFactors,explanation:`Trust graph links passenger, BAG-DNA ID, tags, seal, staff zone, flight and ledger. ${i.explanation}`,recommendedAction:i.recommendedAction}}
